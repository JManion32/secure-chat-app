#ifndef PROTOCOL_QT_HPP
#define PROTOCOL_QT_HPP

#include <QByteArray>
#include "../../common/protocol.hpp"

namespace ProtocolQt {
/*
    QByteArray encodeQt(const Protocol::Message& msg);
    Protocol::Message decodeQt(const QByteArray& arr);
*/
}

#endif
